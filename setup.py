from distutils.core import setup
from setuptools import find_packages

setup(
    name='hdf5_compressor',
    version='0.0.0',
    packages=find_packages(),
    license='None',
    long_description=open('README.md').read(),
)
